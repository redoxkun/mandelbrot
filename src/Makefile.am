bin_PROGRAMS = mandelbrot

mandelbrot_SOURCES = main.cpp image.cpp config.cpp mandelbrot.cpp
mandelbrot_CPPFLAGS = $(MANDELBROT_CFLAGS)

mandelbrot_LDADD = $(MANDELBROT_LIBS)

-include $(top_srcdir)/git.mk
